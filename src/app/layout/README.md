# Layout

`layout` contains application shell components that frame routed pages.

Use this folder for:

- Root shell structure around routed screens.
- Future navigation, app bars, and private-area layout wrappers.
- Shared page spacing and RTL layout decisions.

Layout components can compose shared UI components, but they should not own feature business logic.
