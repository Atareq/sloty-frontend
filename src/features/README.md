# Features

`src/features` owns user-facing feature folders such as auth, dashboard, bookings, clubs, courts, transactions, and settlements when those are implemented.

Keep feature-specific state, forms, API adapters, and screen composition inside the relevant feature folder. Do not move reusable presentational primitives here; put those in `src/shared`.
